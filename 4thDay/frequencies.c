#include<stdio.h>
int frequencies(int arr[], int n){
    int first = arr[0];
    
}

int main(){
    int arr[]={2,4,4,6,6,6,8,7,6,2};
    int n=sizeof(arr)/sizeof(arr[0]);
    int result = frequencies(arr,n);
    printf("%d",result);
}